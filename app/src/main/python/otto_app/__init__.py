"""The Python half of the phone helper: what Kotlin calls, and what it
calls back. Everything Otto-specific reaches this package through
`agent.embed` and `agent.phone`, the two surfaces otto versions its
contract on (see compat.py)."""
