org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Set to true (or pass -PembeddedPython=true) once wheels/ carries the
# Android wheels otto's compiled dependencies need. Off by default so the
# APK builds today; the app then uses the serve transport.
embeddedPython=false
